/* eslint-disable react/prop-types */
import React from 'react';

const ErrorText = (props) => {
    return (
        <div>
            {props.children}
        </div>
    );
};

export default ErrorText;